---
import { BASE } from '../../utils/base';
import { CATEGORY_COLORS } from '../../config/category-colors';

interface Props {
  title: string;
  excerpt: string;
  date: Date;
  id: string;
  image?: string;
  category: string;
  categoryIndex?: number;
  class?: string;
}

const { title, excerpt, date, id, image, category, categoryIndex, class: className = '' } = Astro.props;

const formattedDate = date.toLocaleDateString('en-US', {
  year: 'numeric',
  month: 'short',
  day: 'numeric'
});

const colors = CATEGORY_COLORS[category.toLowerCase()] ?? CATEGORY_COLORS['python'];
---

<article class={`bg-surface-raised border-4 border-foreground shadow-hard-lg card-lift relative overflow-hidden flex flex-col ${className}`}>
  <!-- Geometric decoration in corner -->
  <div class={`absolute top-0 right-0 w-6 h-6 shape-square z-10 ${colors.bg}`}></div>

  {image && (
    <div class="aspect-video overflow-hidden border-b-4 border-foreground flex-shrink-0">
      <img
        src={image}
        alt={title}
        class="w-full h-full object-cover img-bauhaus"
      />
    </div>
  )}

  <div class="p-6 flex flex-col flex-1">
    <div class="flex items-center justify-between gap-3 mb-4">
      <div class="flex items-center gap-3">
        <span class={`text-xs font-bold uppercase tracking-wider px-2 py-1 ${colors.bg} ${colors.text}`}>
          {category}
        </span>
        <time class="text-xs font-medium text-text" datetime={date.toISOString()}>
          {formattedDate}
        </time>
      </div>
      {categoryIndex !== undefined && (
        <span class="text-xs font-black text-text">
          #{categoryIndex}
        </span>
      )}
    </div>

    <h3 class="text-xl font-black uppercase tracking-tight mb-3 leading-tight text-text">
      <a href={`${BASE}/blog/${id}`} class="hover:text-brand-red transition-colors">
        {title}
      </a>
    </h3>

    <p class="text-sm font-medium text-text/70 leading-relaxed mb-4 line-clamp-3 flex-auto">
      {excerpt}
    </p>

    <a
      href={`${BASE}/blog/${id}`}
      class={`inline-flex items-center gap-2 text-sm font-bold uppercase text-text ${colors.hover} transition-colors mt-auto`}
    >
      Read More
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="square" stroke-width="3" d="M5 12h14m-7-7l7 7-7 7" />
      </svg>
    </a>
  </div>
</article>
